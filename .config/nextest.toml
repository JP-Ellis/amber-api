#:schema https://www.schemastore.org/any.json
[profile.ci]
# In CI, we want to run all tests instead of failing fast
fail-fast = false

# Some tests are flaky in CI
retries = { backoff = "exponential", count = 2, delay = "10s" }
